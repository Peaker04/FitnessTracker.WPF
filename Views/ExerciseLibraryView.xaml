<UserControl x:Class="FitnessTracker.WPF.Views.ExerciseLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="40" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"
                     Tag="Tìm kiếm bài tập..." />

            <ComboBox Grid.Column="1" Margin="10,0,0,0" 
                      SelectedItem="{Binding SelectedCategory}"
                      Height="40" VerticalContentAlignment="Center"/>
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding Exercises}" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#EEEEEE" BorderThickness="1" Margin="0,5" Padding="10" CornerRadius="5" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="60" Height="60" Background="#F0F0F0" CornerRadius="5" Margin="0,0,15,0">
                                <TextBlock Text="IMG" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding MuscleGroup}" Foreground="Gray"/>
                            </StackPanel>

                            <Border Grid.Column="2" Background="#E3F2FD" CornerRadius="3" Padding="8,4" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DifficultyLevel}" Foreground="#1976D2" FontSize="12"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>