<UserControl x:Class="FitnessTracker.WPF.Views.WorkoutSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Đang Tập Luyện" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="20"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border BorderBrush="#FF6B35" BorderThickness="5" CornerRadius="100" Width="300" Height="300">
                <Viewbox Stretch="Uniform" Margin="20">
                    <TextBlock Text="{Binding TimerDisplay}" FontWeight="Bold" Foreground="#2C3E50"/>
                </Viewbox>
            </Border>

            <TextBlock Text="Bài tập hiện tại: Push-ups" FontSize="20" HorizontalAlignment="Center" Margin="0,30,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding StartWorkoutCommand}" Content="BẮT ĐẦU" Width="120" Height="50" Margin="10" Background="#06D6A0" Foreground="White" FontSize="16" FontWeight="Bold"/>
            <Button Command="{Binding StopWorkoutCommand}" Content="TẠM DỪNG" Width="120" Height="50" Margin="10" Background="#F77F00" Foreground="White" FontSize="16" FontWeight="Bold"/>
            <Button Command="{Binding ResetTimerCommand}" Content="ĐẶT LẠI" Width="120" Height="50" Margin="10" Background="#CF6679" Foreground="White" FontSize="16" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>