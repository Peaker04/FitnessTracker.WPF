<UserControl x:Class="FitnessTracker.WPF.Views.WorkoutSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Chọn kế hoạch tập hôm nay:" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding AvailablePlans}" 
                      SelectedItem="{Binding SelectedPlan}"
                      DisplayMemberPath="PlanName"
                      Width="250" Height="30" Margin="0,0,0,20"/>

            <Border BorderBrush="#333" BorderThickness="2" CornerRadius="50" Padding="30" Margin="0,0,0,20">
                <TextBlock Text="{Binding TimerDisplay}" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="BẮT ĐẦU" Command="{Binding StartWorkoutCommand}" Width="100" Height="40" Margin="5" Background="Green" Foreground="White"/>
                <Button Content="TẠM DỪNG" Command="{Binding StopWorkoutCommand}" Width="100" Height="40" Margin="5" Background="Orange" Foreground="White"/>
                <Button Content="ĐẶT LẠI" Command="{Binding ResetTimerCommand}" Width="100" Height="40" Margin="5" Background="Gray" Foreground="White"/>
            </StackPanel>

            <Button Content="HOÀN THÀNH &amp; LƯU" 
                    Command="{Binding FinishWorkoutCommand}" 
                    Width="200" Height="50" 
                    Background="#007ACC" Foreground="White" FontSize="16" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>