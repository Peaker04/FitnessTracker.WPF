<UserControl x:Class="FitnessTracker.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <StackPanel Margin="20" MaxWidth="600" HorizontalAlignment="Left">
        <TextBlock Text="Cài Đặt" FontSize="24" FontWeight="Bold" Margin="0,0,0,30"/>

        <GroupBox Header="Thông Tin Cá Nhân" Margin="0,0,0,20" Padding="10">
            <StackPanel>
                <TextBlock Text="Họ và Tên" Margin="0,5"/>
                <TextBox Text="{Binding CurrentUser.FullName}" Height="35" Padding="5"/>

                <TextBlock Text="Email" Margin="0,10,0,5"/>
                <TextBox Text="{Binding CurrentUser.Email}" IsReadOnly="True" Background="#F0F0F0" Height="35" Padding="5"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Cấu Hình AI" Margin="0,0,0,20" Padding="10">
            <StackPanel>
                <CheckBox Content="Bật tính năng hỗ trợ AI" 
                          IsChecked="{Binding IsAIEnabled}" 
                          FontSize="14" Margin="0,5"/>
                <TextBlock Text="Khi bật, AI sẽ gợi ý bài tập và dinh dưỡng dựa trên dữ liệu của bạn." 
                           Foreground="Gray" TextWrapping="Wrap" Margin="20,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <Button Command="{Binding SaveSettingsCommand}" Content="LƯU THAY ĐỔI" 
                Height="40" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
    </StackPanel>
</UserControl>