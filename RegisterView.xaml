<UserControl x:Class="FitnessTracker.WPF.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:FitnessTracker.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="400">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" Margin="40">
            <TextBlock Text="ĐĂNG KÝ TÀI KHOẢN" FontSize="24" FontWeight="Bold" Foreground="#FF6B35" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Username" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="Email" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="Mật khẩu" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPassword" Height="35" Padding="5" Margin="0,0,0,10"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="Nhập lại mật khẩu" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtConfirmPassword" Height="35" Padding="5" Margin="0,0,0,10"
                         PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <Button Content="ĐĂNG KÝ" Command="{Binding RegisterCommand}"
                    Height="40" Background="#FF6B35" Foreground="White" FontWeight="Bold" Cursor="Hand" Margin="0,0,0,10"/>

            <TextBlock HorizontalAlignment="Center">
                <Hyperlink Command="{Binding GoBackCommand}">Quay lại đăng nhập</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>